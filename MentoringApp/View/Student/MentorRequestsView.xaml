<UserControl
    x:Class="MentoringApp.View.Student.MentorRequestsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="clr-namespace:MentoringApp.View.Components"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="700">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock FontSize="26" FontWeight="Bold" Text="📬 Mentoring Requests"/>
            <TextBlock Foreground="#666" FontSize="13"
                       Text="Students who want you as their mentor. Review their profile and decide."/>
        </StackPanel>

        <!-- Status message -->
        <Border Grid.Row="1" Visibility="{Binding HasStatusMessage, Converter={StaticResource BoolToVis}}"
                Background="#E8F5E9" BorderBrush="#4CAF50" BorderThickness="1"
                CornerRadius="4" Padding="12,8" Margin="0,0,0,12">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#1B5E20" FontWeight="SemiBold" TextWrapping="Wrap"/>
        </Border>

        <!-- Loading indicator -->
        <TextBlock Grid.Row="2" Text="Loading requests…" Foreground="#999"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" Margin="0,0,0,8"/>

        <!-- Request list -->
        <ScrollViewer Grid.Row="3">
            <ItemsControl ItemsSource="{Binding PendingRequests}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,10" Padding="16" CornerRadius="8"
                                Background="White" BorderBrush="#E0E0E0" BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" BlurRadius="6" Opacity="0.08"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Avatar -->
                                <components:ProfilePictureControl Grid.Row="0" Grid.RowSpan="2" Grid.Column="0"
                                    Width="52" Height="52" Margin="0,0,14,0" VerticalAlignment="Center"
                                    ImagePath="{Binding MenteeProfilePicturePath}"/>

                                <!-- Info -->
                                <StackPanel Grid.Row="0" Grid.Column="1">
                                    <TextBlock FontSize="15" FontWeight="SemiBold" Text="{Binding MenteeName}"/>
                                    <TextBlock Foreground="#555" FontSize="12" Margin="0,2,0,0">
                                        <Run Text="📗 Wants to learn: "/>
                                        <Run Text="{Binding MenteeSubjectName}" FontWeight="Medium"/>
                                    </TextBlock>
                                    <TextBlock Foreground="#888" FontSize="11" Margin="0,2,0,0"
                                               Text="{Binding CreatedAt, StringFormat='Requested: {0:d}'}"/>
                                </StackPanel>

                                <!-- Accept / Reject buttons -->
                                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,10,0,0">
                                    <Button Margin="0,0,8,0" Padding="14,7" FontWeight="SemiBold"
                                            Background="#2E7D32" Foreground="White" BorderThickness="0" Cursor="Hand"
                                            Command="{Binding DataContext.AcceptRequestCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                        <TextBlock Text="✓ Accept"/>
                                    </Button>
                                    <Button Padding="14,7" FontWeight="SemiBold"
                                            Background="#C62828" Foreground="White" BorderThickness="0" Cursor="Hand"
                                            Command="{Binding DataContext.RejectRequestCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                        <TextBlock Text="✗ Decline"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
