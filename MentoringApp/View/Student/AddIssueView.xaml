<UserControl x:Class="MentoringApp.View.Student.AddIssueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MentoringApp.View.Student"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBox Text="{Binding IssueDescription}" Tag="Issue description"/>
            <ComboBox ItemsSource="{Binding IssueCategoryList}" SelectedItem="{Binding SelectedIssueCategory, ValidatesOnDataErrors=True}" Tag="Issue category"/>
            <Button Content="Add" Command="{Binding AddIssueCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
