<UserControl x:Class="MentoringApp.View.Student.StudentHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:studentvm="clr-namespace:MentoringApp.ViewModel.ViewModelPage.Student;assembly=MentoringApp.ViewModel"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <ListBox ItemsSource="{Binding Pairs}" 
                     SelectedItem="{Binding SelectedPair}">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <Grid Width="40" Height="40" Margin="0,0,10,0">
                                <Ellipse Fill="#E0E0E0" Stroke="Gray" StrokeThickness="1"/>
                                <TextBlock Text="..." 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="#757575"/>
                            </Grid>

                            <TextBlock Text="{Binding Counterpart.Name}" 
                       VerticalAlignment="Center" 
                       FontSize="14"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>

        <ContentControl Grid.Row="1" Content="{Binding SelectedPair}">
            <ContentControl.Resources>

                <DataTemplate DataType="{x:Type studentvm:MentorDashboardViewModel}">
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Top">
                            <TextBlock Text="{Binding Counterpart.UserName}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding CounterpartRole}"/>
                            <ProgressBar Value="{Binding MenteeProgress}" Height="10"/>
                        </StackPanel>

                        <Button Content="Add Issue" Command="{Binding AddIssueCommand}" DockPanel.Dock="Bottom"/>

                        <ListView ItemsSource="{Binding MyIssues}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Description}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </DockPanel>
                </DataTemplate>

                <DataTemplate DataType="{x:Type studentvm:MenteeDashboardViewModel}">
                    <StackPanel>
                        <TextBlock Text="{Binding Counterpart.UserName}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding CounterpartRole}"/>
                    </StackPanel>
                </DataTemplate>

            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</UserControl>