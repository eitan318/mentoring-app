<UserControl x:Class="MentoringApp.View.Supervisor.PairDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MentoringApp.View.Supervisor"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock FontSize="24" FontWeight="Bold" VerticalAlignment="Center">
                <Run Text="Pair: "/>
                <Run Text="{Binding CurrentPair.Mentor.UserName}"/>
                <Run Text=" &amp; "/>
                <Run Text="{Binding CurrentPair.Mentee.UserName}"/>
            </TextBlock>
        </DockPanel>

        <UniformGrid Grid.Row="2" Columns="2">
            
            <DockPanel Margin="0,0,10,0">
                <TextBlock DockPanel.Dock="Top" FontSize="18" FontWeight="SemiBold" Text="Recent Reviews" Margin="0,0,0,10"/>
                <ListView ItemsSource="{Binding PairReviews}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5">
                                <TextBlock Text="{Binding Date, StringFormat='{}Date: {0:d}'}" FontWeight="Medium"/>
                                <TextBlock Text="{Binding Content}" TextWrapping="Wrap" Margin="0,5,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </DockPanel>

            <DockPanel Margin="10,0,0,0">
                <TextBlock DockPanel.Dock="Top" FontSize="18" FontWeight="SemiBold" Text="Issues" Margin="0,0,0,10"/>
                <ListView ItemsSource="{Binding PairIssues}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5">
                                <TextBlock FontWeight="Medium" Text="{Binding Description}" TextWrapping="Wrap"/>
                                <StackPanel Orientation="Horizontal" Opacity="0.8">
                                    <TextBlock Text="{Binding Category.Name}" FontStyle="Italic" Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding CreationDate, StringFormat='{}Created: {0:d}'}"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </DockPanel>

        </UniformGrid>
    </Grid>
</UserControl>
