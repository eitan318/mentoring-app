<UserControl x:Class="MentoringApp.View.Supervisor.SupervisorDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="{Binding SelectedSupervisor.UserName}" FontSize="24" FontWeight="Bold"/>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Total Pairs Supervised:" Grid.Column="0" Margin="5" FontWeight="Medium"/>
                <TextBlock Text="{Binding PairsSupervised.Count}" Grid.Column="1" Margin="5"/>
            </Grid>

            <UniformGrid Columns="2">
                <DockPanel Margin="0,0,10,0">
                    <TextBlock Text="Pending Issues" DockPanel.Dock="Top" Foreground="DarkRed" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <ListBox ItemsSource="{Binding PendingIssues}" BorderBrush="Tomato" BorderThickness="1">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="2">
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding CreatedAt, StringFormat='{}Created: {0:d}'}" FontSize="10" Foreground="Gray"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>

                <DockPanel Margin="10,0,0,0">
                    <TextBlock Text="Resolved Issues" DockPanel.Dock="Top" Foreground="DarkGreen" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <ListBox ItemsSource="{Binding ResolvedIssues}" BorderBrush="LightGreen" BorderThickness="1">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="2">
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" TextDecorations="Strikethrough" Foreground="Gray"/>
                                    <TextBlock Text="Status: Resolved" FontSize="10" Foreground="Green"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
                
                
                
            </UniformGrid>

            <ListView Grid.Row="1" ItemsSource="{Binding PairsSupervised}" Margin="5">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <TextBlock Text="Mentor: " FontWeight="Bold"/>
                            <TextBlock Text="{Binding Mentor.UserName}" Margin="0,0,10,0"/>

                            <TextBlock Text="Mentee: " FontWeight="Bold"/>
                            <TextBlock Text="{Binding Mentee.UserName}" Margin="0,0,10,0"/>

                            <Button Content="Separate" 
                                Command="{Binding DataContext.SeparateCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                CommandParameter="{Binding}"
                                Padding="10,2"/>

                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Button Content="Back to List" 
                    Command="{Binding BackCommand}" 
                    HorizontalAlignment="Left" 
                    Padding="15,5"
                    Margin="0,20"/>
        </StackPanel>
    </Grid>
</UserControl>