<UserControl x:Class="MentoringApp.View.Admin.ManageUsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:model="clr-namespace:MentoringApp.Model;assembly=MentoringApp.Model"
             xmlns:components="clr-namespace:MentoringApp.View.Components"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="500">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Search Users:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     FontSize="14" Padding="5" Margin="0,0,0,15"/>

            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding RegisterSupervisorCommand}" Content="Add Supervisor" Padding="8,4" Margin="0,0,5,0"/>
                <Button Command="{Binding RegisterStudentCommand}" Content="Add Student" Padding="8,4" Margin="0,0,5,0"/>
                <Button Command="{Binding DeleteUserCommand}" Content="Delete Selected" Padding="8,4" Background="#FFEDD4D4"/>
                <Button Command="{Binding ViewUserCommand}" Content="View Selected" Padding="8,4" Background="#FFEDD4D4"/>
            </StackPanel>
        </StackPanel>

        <ListBox Grid.Row="1" 
                 ItemsSource="{Binding FilteredUsers}" 
                 SelectedItem="{Binding SelectedUser}"
                 HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type model:User.UserModel}">
                    <Border BorderBrush="#EEE" BorderThickness="0,0,0,1" Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!--  Small circular avatar  -->
                            <components:ProfilePictureControl Grid.Column="0" Width="40" Height="40" Margin="0,0,10,0" VerticalAlignment="Center" ImagePath="{Binding ProfilePicturePath}" />

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding UserName}" FontWeight="Bold" FontSize="16"/>
                                <TextBlock Text="{Binding Email}" Foreground="Gray" FontSize="12"/>
                            </StackPanel>

                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>