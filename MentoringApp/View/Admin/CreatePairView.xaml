<UserControl x:Class="MentoringApp.View.Admin.CreatePairView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MentoringApp.View.Admin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Select Supervisor:"/>
            <ComboBox ItemsSource="{Binding AvailableSupervisors}" 
              SelectedItem="{Binding SelectedSupervisor}" 
              DisplayMemberPath="UserName" 
              Margin="0,0,0,10"/>

            <TextBlock Text="Select Mentor:"/>
            <ComboBox ItemsSource="{Binding AvailableMentors}" 
              SelectedItem="{Binding SelectedMentor}" 
              DisplayMemberPath="UserName" 
              Margin="0,0,0,10"/>

            <TextBlock Text="Select Mentee:"/>
            <ComboBox ItemsSource="{Binding AvailableMentees}" 
              SelectedItem="{Binding SelectedMentee}" 
              DisplayMemberPath="UserName" 
              Margin="0,0,0,10"/>

            <Button Content="Create Pair" 
                Command="{Binding CreatePairCommand}" 
                Height="30" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
