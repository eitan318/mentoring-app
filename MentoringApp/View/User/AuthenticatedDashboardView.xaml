<UserControl x:Class="MentoringApp.View.User.AuthenticatedDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MentoringApp.View.User"
             mc:Ignorable="d" 
             >
        <DockPanel>
        <Grid DockPanel.Dock="Top" Background="#F5F5F5" Height="40">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="MENTOR APP" VerticalAlignment="Center" Margin="15,0" FontWeight="Bold"/>
                <Button Content="{Binding CurrentUser.UserName}" Command="{Binding NavigateProfileCommand}" Visibility="{Binding IsProfileButtonVisible, Converter={StaticResource BoolToVis}}"/>
                <Button Content="logout" Command="{Binding LogoutCommand}"/>
                <Button Content="Back" Command="{Binding BackCommand}" Visibility="{Binding IsBackVisible, Converter={StaticResource BoolToVis}}"/>
                </StackPanel>
            </Grid>

        <ContentControl Content="{Binding ActiveSubPage}" />
    </DockPanel>
                
    </UserControl>
