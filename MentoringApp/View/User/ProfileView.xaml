<UserControl x:Class="MentoringApp.View.User.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:m="clr-namespace:MentoringApp.Model;assembly=MentoringApp.Model">
    <StackPanel Margin="10">

        <Label Content="Username"/>
        <TextBox Text="{Binding CurrentUser.UserName}" IsReadOnly="{Binding IsReadOnly}"/>

        <Label Content="Email"/>
        <TextBox Text="{Binding CurrentUser.Email}" IsReadOnly="{Binding IsReadOnly}"/>

        <Label Content="National Id"/>
        <TextBox Text="{Binding CurrentUser.NationalId}" IsReadOnly="True"/>



        <ContentControl Content="{Binding User}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type m:Student}">
                    <StackPanel>
                        <Label Content="Subject to Teach"/>
                        <TextBox Text="{Binding MentorProfile.SubjectToTeach}" 
                                 IsReadOnly="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

                        <Label Content="Subject to Learn"/>
                        <TextBox Text="{Binding MenteeProfile.SubjectToLearn}" 
                                 IsReadOnly="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

                    </StackPanel>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>

        <Grid Margin="0,20,0,0">
            <Button Content="Edit Profile" 
                    Command="{Binding ToggleEditCommand}"
                    Visibility="{Binding IsReadOnly, Converter={StaticResource BoolToVis}}"/>

            <Button Content="Save Changes" 
                    Command="{Binding SaveCommand}"
                    Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVis}}"/>
        </Grid>

    </StackPanel>
</UserControl>