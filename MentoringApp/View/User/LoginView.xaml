<UserControl x:Class="MentoringApp.View.User.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MentoringApp.View.User"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <StackPanel>
            <StackPanel Visibility="{Binding WasCodeSent, Converter={StaticResource InverseBoolToVis}}">
                <TextBlock Text="National Id:" Margin="0,5"/>
                <TextBox Text="{Binding NationalId, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                <Button Content="Send Verification Code" 
            Command="{Binding SendVerificationCodeCommand}" 
            Margin="0,10"/>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" MaxWidth="200"/>
            </StackPanel>

            <StackPanel Visibility="{Binding WasCodeSent, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="Verification code:" Margin="0,5"/>
                <TextBox Text="{Binding VerificationCode, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                <Button Content="Login" 
            Command="{Binding LoginCommand}" 
            Margin="0,10"/>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" MaxWidth="200"/>
            </StackPanel>
        </StackPanel>
        
    </Grid>
</UserControl>