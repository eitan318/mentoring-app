<UserControl
    x:Class="MentoringApp.View.Student.AddReviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MentoringApp.View.Student"
    xmlns:loc="clr-namespace:MentoringApp.Localization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    FlowDirection="{Binding Source={x:Static loc:TranslationSource.Instance}, Path=FlowDirection}">

    <UserControl.Resources>
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#059669"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F4F7FE" FocusManager.FocusedElement="{Binding ElementName=commentInput}">
        <Border
            Background="White"
            CornerRadius="16"
            Padding="28"
            MaxWidth="480"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Color="#000000" Opacity="0.06" ShadowDepth="2" Direction="270"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="#111827"
                    Margin="0,0,0,20"
                    Text="{Binding [Review_Content_Label], Source={x:Static loc:TranslationSource.Instance}}" />

                <TextBlock
                    FontSize="12"
                    FontWeight="SemiBold"
                    Foreground="#374151"
                    Margin="0,0,0,6"
                    Text="{Binding [Review_Content_Label], Source={x:Static loc:TranslationSource.Instance}}" />
                <TextBox
                    x:Name="commentInput"
                    MinHeight="100"
                    Padding="10,8"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    FontSize="14"
                    Margin="0,0,0,16"
                    AcceptsReturn="True"
                    Tag="{Binding [Review_Content_Label], Source={x:Static loc:TranslationSource.Instance}}"
                    Text="{Binding ReviewContent, UpdateSourceTrigger=PropertyChanged}"
                    TextWrapping="Wrap" />

                <TextBlock
                    FontSize="12"
                    FontWeight="SemiBold"
                    Foreground="#374151"
                    Margin="0,0,0,6"
                    Text="{Binding [Review_Hours_Label], Source={x:Static loc:TranslationSource.Instance}}" />
                <TextBox
                    Width="120"
                    HorizontalAlignment="Left"
                    Padding="10,8"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    FontSize="14"
                    Margin="0,0,0,24"
                    Text="{Binding AmountOfHours, UpdateSourceTrigger=PropertyChanged}"
                    ToolTip="Enter the number of meeting hours (e.g. 1.5)" />

                <Button
                    HorizontalAlignment="Right"
                    Style="{StaticResource PrimaryBtn}"
                    Command="{Binding AddReviewCommand}"
                    Content="{Binding [Review_Submit_Button], Source={x:Static loc:TranslationSource.Instance}}"
                    IsDefault="True" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
