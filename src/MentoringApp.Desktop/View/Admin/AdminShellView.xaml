<UserControl
    x:Class="MentoringApp.View.Admin.AdminShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:loc="clr-namespace:MentoringApp.Localization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="750" d:DesignWidth="900"
    mc:Ignorable="d"
    FlowDirection="{Binding Source={x:Static loc:TranslationSource.Instance}, Path=FlowDirection}">

    <UserControl.Resources>
        <Style x:Key="SideNavItem" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#BFDBFE"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,11"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#1976D2"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="175"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ═══ Left Sidebar ═══ -->
        <Border Grid.Column="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#1565C0" Offset="0"/>
                    <GradientStop Color="#0D47A1" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <DockPanel>
                <!-- Sidebar header -->
                <Border DockPanel.Dock="Top" Padding="20,18,20,14"
                        BorderThickness="0,0,0,1" BorderBrush="#1976D2">
                    <StackPanel>
                        <TextBlock Text="ADMIN" FontSize="10" FontWeight="Bold"
                                   Foreground="#93C5FD" Margin="0,0,0,3"/>
                        <TextBlock Text="{Binding [App_Title], Source={x:Static loc:TranslationSource.Instance}}"
                                   FontSize="14" FontWeight="Bold" Foreground="White"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Nav items -->
                <StackPanel DockPanel.Dock="Top" Margin="0,8,0,0">

                    <!-- Dashboard -->
                    <Grid>
                        <Border HorizontalAlignment="Left" Width="3" Background="White" VerticalAlignment="Stretch">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ActivePage}" Value="dashboard">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                        <Button Command="{Binding GoToDashboardCommand}">
                            <Button.Content>
                                <TextBlock Text="{Binding [Admin_Dashboard_Button], Source={x:Static loc:TranslationSource.Instance}}"/>
                            </Button.Content>
                            <Button.Style>
                                <Style BasedOn="{StaticResource SideNavItem}" TargetType="Button">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ActivePage}" Value="dashboard">
                                            <Setter Property="Background" Value="#1565C0"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>

                    <!-- Manage Pairs -->
                    <Grid>
                        <Border HorizontalAlignment="Left" Width="3" Background="White" VerticalAlignment="Stretch">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ActivePage}" Value="pairs">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                        <Button Command="{Binding GoToManagePairsCommand}">
                            <Button.Content>
                                <TextBlock Text="{Binding [Admin_ManagePairs_Button], Source={x:Static loc:TranslationSource.Instance}}"/>
                            </Button.Content>
                            <Button.Style>
                                <Style BasedOn="{StaticResource SideNavItem}" TargetType="Button">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ActivePage}" Value="pairs">
                                            <Setter Property="Background" Value="#1565C0"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>

                    <!-- Manage Users -->
                    <Grid>
                        <Border HorizontalAlignment="Left" Width="3" Background="White" VerticalAlignment="Stretch">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ActivePage}" Value="users">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                        <Button Command="{Binding GoToManageUsersCommand}">
                            <Button.Content>
                                <TextBlock Text="{Binding [Admin_ManageUsers_Button], Source={x:Static loc:TranslationSource.Instance}}"/>
                            </Button.Content>
                            <Button.Style>
                                <Style BasedOn="{StaticResource SideNavItem}" TargetType="Button">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ActivePage}" Value="users">
                                            <Setter Property="Background" Value="#1565C0"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>

                    <Rectangle Height="1" Fill="#1976D2" Margin="20,6"/>

                    <!-- System Settings -->
                    <Grid>
                        <Border HorizontalAlignment="Left" Width="3" Background="White" VerticalAlignment="Stretch">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ActivePage}" Value="settings">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                        <Button Command="{Binding GoToSystemSettingsCommand}">
                            <Button.Content>
                                <TextBlock Text="{Binding [Admin_SystemSettings_Button], Source={x:Static loc:TranslationSource.Instance}}"/>
                            </Button.Content>
                            <Button.Style>
                                <Style BasedOn="{StaticResource SideNavItem}" TargetType="Button">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ActivePage}" Value="settings">
                                            <Setter Property="Background" Value="#1565C0"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- ═══ Content Area ═══ -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentAdminPage}"/>
    </Grid>
</UserControl>
