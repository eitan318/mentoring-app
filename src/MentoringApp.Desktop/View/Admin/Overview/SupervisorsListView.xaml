<UserControl
    x:Class="MentoringApp.View.Admin.Overview.SupervisorsListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="clr-namespace:MentoringApp.ViewModel.Localization;assembly=MentoringApp.ViewModel"
    xmlns:components="clr-namespace:MentoringApp.View.Components"
    FlowDirection="{Binding Source={x:Static loc:TranslationSource.Instance}, Path=FlowDirection}">

    <Border Padding="20,20,20,0">
        <Border.Style>
            <Style TargetType="Border" BasedOn="{StaticResource CardStyle}">
                <Setter Property="Visibility" Value="Collapsed"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Progress.IsRegistrationStepActive}" Value="True">
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Progress.IsMentorSelectionStepActive}" Value="True">
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Progress.IsProcessComplete}" Value="True">
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Table Header: normal phases (Pairs / Pending / Resolved) -->
            <Grid Grid.Row="1" Margin="0,0,0,12">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Progress.IsRegistrationStepActive}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding [Table_Supervisor], Source={x:Static loc:TranslationSource.Instance}}" FontWeight="SemiBold" FontSize="13" Foreground="#6B7280"/>
                <TextBlock Grid.Column="1" Text="{Binding [Table_Pairs], Source={x:Static loc:TranslationSource.Instance}}" FontWeight="SemiBold" FontSize="13" Foreground="#6B7280" HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="{Binding [Table_Pending], Source={x:Static loc:TranslationSource.Instance}}" FontWeight="SemiBold" FontSize="13" Foreground="#6B7280" HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="3" Text="{Binding [Table_Resolved], Source={x:Static loc:TranslationSource.Instance}}" FontWeight="SemiBold" FontSize="13" Foreground="#6B7280" HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="4" Text="{Binding [Table_Action], Source={x:Static loc:TranslationSource.Instance}}" FontWeight="SemiBold" FontSize="13" Foreground="#6B7280" Width="80" TextAlignment="Center"/>
            </Grid>

            <!-- Table Header: registration phase (Registered count) -->
            <Grid Grid.Row="1" Margin="0,0,0,12">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Progress.IsRegistrationStepActive}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding [Table_Supervisor], Source={x:Static loc:TranslationSource.Instance}}" FontWeight="SemiBold" FontSize="13" Foreground="#6B7280"/>
                <TextBlock Grid.Column="1" Text="{Binding [Table_Registered], Source={x:Static loc:TranslationSource.Instance}}" FontWeight="SemiBold" FontSize="13" Foreground="#6B7280" HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="{Binding [Table_Action], Source={x:Static loc:TranslationSource.Instance}}" FontWeight="SemiBold" FontSize="13" Foreground="#6B7280" Width="80" TextAlignment="Center"/>
            </Grid>

            <Rectangle Grid.Row="1" Height="1" Fill="#F3F4F6" VerticalAlignment="Bottom" Margin="-20,0,-20,0"/>

            <!-- Supervisors List -->
            <ListView Grid.Row="2" ItemsSource="{Binding SupervisorsListPreview}"
                      BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0,16"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#F3F4F6"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <!-- Normal-phase row (Pairs / Pending / Resolved) -->
                            <Grid>
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding DataContext.Progress.IsRegistrationStepActive, RelativeSource={RelativeSource AncestorType=ListView}}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <components:ProfilePictureControl
                                        Width="32" Height="32" Margin="0,0,12,0"
                                        ImagePath="{Binding ProfilePicturePath}"
                                        Gender="{Binding Gender}"/>
                                    <TextBlock Text="{Binding UserName}" VerticalAlignment="Center" FontWeight="Medium" Foreground="#111827"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1" Text="{Binding SupervisedPairsCount}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#4B5563"/>
                                <TextBlock Grid.Column="2" Text="{Binding PendingIssuesCount}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#EF4444" FontWeight="Medium"/>
                                <TextBlock Grid.Column="3" Text="{Binding ResolvedIssuesCount}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#10B981" FontWeight="Medium"/>

                                <Button Grid.Column="4" Content="{Binding [Table_Inspect], Source={x:Static loc:TranslationSource.Instance}}" Width="80" Padding="0,6"
                                        Background="#F3F4F6" Foreground="#4B5563" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"
                                        Command="{Binding DataContext.InspectSupervisorCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}">
                                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                                </Button>
                            </Grid>

                            <!-- Registration-phase row (Registered count) -->
                            <Grid>
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding DataContext.Progress.IsRegistrationStepActive, RelativeSource={RelativeSource AncestorType=ListView}}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <components:ProfilePictureControl
                                        Width="32" Height="32" Margin="0,0,12,0"
                                        ImagePath="{Binding ProfilePicturePath}"
                                        Gender="{Binding Gender}"/>
                                    <TextBlock Text="{Binding UserName}" VerticalAlignment="Center" FontWeight="Medium" Foreground="#111827"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1" Text="{Binding FillProgressLabel}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#4B5563" FontWeight="Medium"/>

                                <Button Grid.Column="2" Content="{Binding [Table_Inspect], Source={x:Static loc:TranslationSource.Instance}}" Width="80" Padding="0,6"
                                        Background="#F3F4F6" Foreground="#4B5563" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"
                                        Command="{Binding DataContext.InspectSupervisorCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}">
                                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                                </Button>
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Border>
</UserControl>
