<UserControl x:Class="MentoringApp.View.User.OtherProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:components="clr-namespace:MentoringApp.View.Components"
             xmlns:loc="clr-namespace:MentoringApp.Localization"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="400"
             FlowDirection="{Binding Source={x:Static loc:TranslationSource.Instance}, Path=FlowDirection}">

    <Grid Margin="20">
        <StackPanel>
            <!--  Circular profile picture  -->
            <components:ProfilePictureControl Width="80" Height="80" HorizontalAlignment="Center" Margin="0,0,0,10" ImagePath="{Binding ProfilePicturePath}" Gender="{Binding Gender}" />

            <TextBlock Text="{Binding UserName}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding RoleName}" FontSize="14" Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <Separator Margin="0,10"/>

            <Label Content="{Binding [OtherProfile_Email_Label], Source={x:Static loc:TranslationSource.Instance}}" Foreground="Gray" FontSize="11"/>
            <TextBlock Text="{Binding Email}" FontSize="16" Margin="5,0,0,15"/>

            <Label Content="{Binding [OtherProfile_PhoneNumber_Label], Source={x:Static loc:TranslationSource.Instance}}" Foreground="Gray" FontSize="11"/>
            <TextBlock Text="{Binding PhoneNumber}" FontSize="16" Margin="5,0,0,15"/>

            <Label Content="{Binding [OtherProfile_Gender_Label], Source={x:Static loc:TranslationSource.Instance}}" Foreground="Gray" FontSize="11"/>
            <TextBlock Text="{Binding GenderDisplay}" FontSize="16" Margin="5,0,0,15"/>

            <StackPanel Visibility="{Binding IsStudent, Converter={StaticResource BoolToVis}}">
                <Label Content="{Binding [OtherProfile_Grade_Label], Source={x:Static loc:TranslationSource.Instance}}" Foreground="Gray" FontSize="11"/>
                <TextBlock FontSize="16" Margin="5,0,0,15">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource GradeClassDisplay}">
                            <Binding Path="GradeName"/>
                            <Binding Path="ClassNum"/>
                            <Binding Source="{x:Static loc:TranslationSource.Instance}" Path="CurrentCulture"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>

                <Label Content="{Binding [OtherProfile_PreferredMentorGender_Label], Source={x:Static loc:TranslationSource.Instance}}" Foreground="Gray" FontSize="11"/>
                <TextBlock Text="{Binding PreferredMentorGenderDisplay}" FontSize="16" Margin="5,0,0,15"/>

                <Label Content="{Binding [OtherProfile_PreferredMenteeGender_Label], Source={x:Static loc:TranslationSource.Instance}}" Foreground="Gray" FontSize="11"/>
                <TextBlock Text="{Binding PreferredMenteeGenderDisplay}" FontSize="16" Margin="5,0,0,15"/>

                <Label Content="{Binding [OtherProfile_MentoringDetails_Label], Source={x:Static loc:TranslationSource.Instance}}" Foreground="Gray" FontSize="11"/>
                <Border Background="#F9F9F9" Padding="10" Margin="5,0,0,15" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="{Binding TeachingSubject}" Margin="0,2"/>
                        <TextBlock Text="{Binding LearningSubject}" Margin="0,2"/>
                    </StackPanel>
                </Border>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>